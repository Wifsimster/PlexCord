package history

// Placeholder so `go test -covermode=atomic ./...` does not invoke `covdata`
// against this package — the Go 1.25.0 auto-downloaded toolchain ships
// without the covdata tool, which trips packages that have no real tests.
